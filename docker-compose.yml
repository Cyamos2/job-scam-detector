version: '3.8'
services:
  scamicide-server:
    build: ./server
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
    volumes:
      - ./server/logs:/app/logs
      - ./server/data:/app/data
    restart: unless-stopped
